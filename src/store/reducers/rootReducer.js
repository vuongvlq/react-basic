const initState = {
  users: [
    { id: 1, name: 'user 1' },
    { id: 2, name: 'user 2' },
    { id: 3, name: 'user 3' }
  ]
}

const rootReducer = (state = initState, action) => {

  return state
}

export default rootReducer